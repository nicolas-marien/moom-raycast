# Moom

Control Moom

## Commands

- `List Layouts`
  - Lists available Moom layouts
  - Apply selected layout
  - Add or remove favorites with `Command+F`
- `Apply Favorite Layout`
  - Lists favorite layouts only
  - Apply quickly from a short list
- `Save Current Layout`
  - Saves currently visible windows as a Moom layout
  - Choose `Replace` (default) or `Merge`
  - Asks for confirmation before replacing an existing layout

## Requirements

- Moom must be installed and running
- Raycast needs permission to automate Moom (macOS may prompt on first run)

## Branding Note

- This project currently uses the official Moom logo for local/development use.
- Before publishing to the Raycast Store, confirm trademark/logo permission with Many Tricks or switch to a custom icon.
